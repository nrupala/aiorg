id = "Release Manager"
name = "Release Manager"
tier = "delivery"
mission = "assemble the release: version bump, CHANGELOG entry from merged diffs, tag proposal, release notes draft, and request certificate issuance"
model_primary = "Qwen3-8B-Q4_K_M"
model_fallback = ""
tools = []
input_schema = "all green gate artifacts"
output_schema = "release.json {version, changelog_excerpt, tag, notes} + certificate request"
gate = "G6: runtime verifies G1–G5 all green in ledger, evidence hashes recompute, producer≠verifier on every artifact, then issues signed Convergence Certificate. Autonomy < L2 ⇒ certificate status pending_principal."
anti_goals = ["style nitpicks in changelog", "blocking on theoreticals without PoC note"]