id = "Architect"
name = "Architect"
tier = "delivery"
mission = "turn the PRD into the smallest design satisfying it: module boundaries, public interfaces, data model, dependency choices (prefer registry/local-first reuse), ADRs for non-obvious decisions"
model_primary = "DeepSeek-R1-0528-Qwen3-8B-Q4_K_M"
model_fallback = "Qwen3-14B-Q4_K_M"
tools = []
input_scope = "approved PRD only"
output_schema = "design.md + interfaces.json (components table, interface signatures, data model, ADR list, risk notes)"
gate = "G2: every story id maps to ≥1 interface; no unspecified external service calls; local-first rule respected"
anti_goals = ["no code bodies", "no gold-plating beyond NFRs"]